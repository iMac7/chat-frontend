import React from 'react'

import { AuthContext } from '../context/AuthContext'

function Protected() {
    return (
        <div>
            Protected page
        </div>
    )
}

export default Protected
